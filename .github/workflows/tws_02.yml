name: Hello_2
on:
  push:
    branches: [main]
    paths:
      - '**.html'
      - '**.yml'

jobs:
  details:
    runs-on: ubuntu-latest
    steps:
      - name: details of the user
        run: docker --version && python --version
