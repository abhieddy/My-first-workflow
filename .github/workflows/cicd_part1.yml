name: cicd-part1

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: code checout
        run: echo "code is got checkout"

  build:
    needs: code
    runs-on: ubuntu-latest
    steps:
      - name: build code
        run: echo "build is successful"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: test the app
        run: echo "test is successful"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: deploy the code
        run: echo "deployment is done"

    